# Google Service Account Authentication
# Path to your service account JSON credentials file
GOOGLE_APPLICATION_CREDENTIALS=

# Google Sheets Configuration
# The ID of your Google Sheets document (found in the URL)
SHEET_ID=

# The name of the specific worksheet/tab to read
WORKSHEET_NAME=

# Optional: Google Sheets API scope (defaults to readonly)
# SHEETS_SCOPE=https://www.googleapis.com/auth/spreadsheets.readonly

# Optional: Request timeout in seconds (default: 10)
# REQUEST_TIMEOUT=10

# Optional: Maximum retry attempts (default: 3)
# RETRY_MAX_ATTEMPTS=3

# Optional: Initial retry delay in seconds (default: 5)
# RETRY_INITIAL_DELAY=5

# Optional: Watch mode interval in seconds (default: 300)
# WATCH_INTERVAL=300

# Optional: Jitter percentage for watch intervals (default: 10)
# JITTER_PERCENT=10

# Database URL for API (Vercel functions)
# DATABASE_URL=https://your-secure-database-url